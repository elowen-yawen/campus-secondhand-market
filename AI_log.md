* **使用的 AI 工具**：DeepSeek、Gemini

---

## 一、环境配置

### 1. 跨平台启动脚本编写

* **提示词**：
  > 为项目编写跨平台启动脚本，覆盖 Windows、Linux 与 macOS 环境，脚本内置环境检测与虚拟环境自动配置逻辑。
* **AI 原始输出关键部分**：
  > “已完成启动脚本的编写... 创建的文件：`start.sh` — Linux/macOS 启动脚本，`start.bat` — Windows 启动脚本。两个脚本功能一致，均会自动完成：1. 检测 Python 3 环境 2. 创建虚拟环境... 5. 启动服务 (`python -m src.main`)”
* **人工修改说明**：
  接受了 AI 生成的启动脚本代码，并在本地 macOS 环境中人工赋予了脚本执行权限（`chmod +x start.sh`），测试验证了能够顺利一键拉起 Flask 后端服务。
* **反思与收获**：
  AI 极大简化了跨平台环境配置的繁琐度，能迅速生成包含依赖安装和虚拟环境创建的标准 Shell/Bat 脚本。局限性在于有时无法完全预测本地用户的系统权限设置，仍需人工进行少量的权限调整干预。

### 2. Python 依赖缺失问题排查

* **提示词**：
  > 在启动后端服务时遇到 `ModuleNotFoundError: No module named 'dotenv'`，该如何解决 Python 依赖缺失的问题？
* **AI 原始输出关键部分**：
  > “错误表明 Python 环境中缺少 `python-dotenv` 模块... 解决方法：1. 进入后端项目根目录 2. 执行命令：`pip install -r requirements.txt`... 检查是否在虚拟环境中：如果项目使用了虚拟环境（例如 `.venv`），需要先激活它然后再安装依赖。”
* **人工修改说明**：
  按照 AI 的排查思路，手动检查了终端，确认并激活了对应的 `.venv` 虚拟环境，重新运行了依赖安装命令补全了丢失的库文件。
* **反思与收获**：
  AI 能够根据堆栈报错迅速定位依赖缺失原因，并给出国内镜像源加速的备选方案。但对于复杂的本地多 Python 冲突环境，依然需要开发者自己弄清楚当前终端挂载的具体是哪个 environment。

---

## 二、核心功能实现

### 3. 后端架构与接口规划

* **提示词**：
  > 请你根据这个项目的后端部分，完成当前项目的后端 (`back` 目录) 的说明 (`README.md`)，要求：使用 Flask 编写核心业务接口（如用户登录、二手商品列表、商品详情等）。数据库采用 SQLite，Python 内使用 SQLModel 进行连接。这一步只进行后端的项目规划，写出 README 和 TODO 即可。
* **AI 原始输出关键部分**：
  > “`README` 和 `TODO` 已在 `back` 目录完成，内容覆盖技术选型、接口规划、数据模型与待办清单... 接下来要继续的话，你想先做哪一步？1. 根据 `README` 的结构生成最小可运行骨架 2. 细化接口文档...”
* **人工修改说明**：
  结合二手交易平台的实际需求，人工审查了 AI 规划的 12 张数据模型表（如 `User`, `Goods`, `TradeOrder` 等）和业务逻辑模块划分。确认无误后，指示 AI 根据此文档继续生成后端具体代码。
* **反思与收获**：
  AI 非常擅长根据自然语言的需求进行系统性设计，可以快速产出高质量的项目骨架规划。局限性是：如果开发者缺乏架构意识去严格审查 AI 设计的表结构，可能会在后期功能扩展时遇到瓶颈。

### 4. SQLite 后端适配与安全加固

* **提示词**：
  > 参考指定开源项目的后端实现，为当前二手交易平台适配 SQLite 版本的后端，并说明关键适配点。
* **AI 原始输出关键部分**：
  > “后端已完整搭建。项目结构：`back/src/main.py`、`db.py` 等，已按 `API.md` 定义实现全部 47 个接口，覆盖用户认证、商品、订单、聊天等八大模块，路由与业务逻辑解耦，注释统一为中文。”
* **人工修改说明**：
  对 AI 生成的后端代码按模块进行全流程 Review，重点补全了登录鉴权逻辑，通过补充提示词完善了 JWT 校验装饰器，并将其绑定到受保护路由上。
* **反思与收获**：
  AI 在批量生成 CRUD 接口时效率优势明显，但对权限控制这类全局安全逻辑容易缺失。开发者必须主动进行代码审查，并通过多轮交互驱动 AI 补全安全校验环节，避免上线后出现权限漏洞。

---

## 三、错误调试

### 5. 身份认证未携带导致的 401 错误

* **提示词**：
  > Request URL `http://127.0.0.1:5000/api/chat/sessions?userId=1&pageSize=50` Request Method GET Status Code 401 UNAUTHORIZED... 这个 401 错误是什么错误？
* **AI 原始输出关键部分**：
  > “这是一个 “未经授权” 错误... 在 `frontend/components/tab-bar.vue` 文件中，获取未读消息的代码是直接使用了 `uni.request`，而没有包含登录后获取到的 `Authorization (Token)` 请求头。修复方案：将原来的 `uni.request` 替换为 `get` 方法，自动携带 Token。”
* **人工修改说明**：
  采纳了 AI 提供的修改方案，将 `tab-bar.vue` 中的硬编码请求替换为统一封装好的 `get()` 方法，彻底解决了因未携带身份令牌导致的轮询报错问题。
* **反思与收获**：
  只需提供网络请求控制台的报错日志和接口地址，AI 就能逆向推理并精准定位到前端代码中被遗漏的 Headers 拦截器配置，极大地节省了 Debug 的时间。

### 6. 前后端分离架构下的 CORS 跨域问题

* **提示词**：
  > 问题4：前端请求后端接口时出现 CORS 跨域错误，浏览器提示 `No 'Access-Control-Allow-Origin' header`，在不修改后端代码的前提下有没有临时绕过的方法？若必须修改后端，需要添加什么配置？
* **AI 原始输出关键部分**：
  > “临时绕过方法：在 `manifest.json` 的 `h5` 配置中添加 `devServer` 代理... 根本解决方法是修改后端，添加 CORS 配置：安装 `Flask-CORS`，在 `main.py` 中初始化 `CORS(app, supports_credentials=True, origins='*', allow_headers=['Content-Type', 'Authorization'])`”
* **人工修改说明**：
  为了彻底解决前后端分离架构下的跨域联调问题，放弃了前端代理的临时方案，人工介入在后端 `main.py` 中引入了 `flask_cors` 库并应用全局配置。
* **反思与收获**：
  AI 能够同时从前端和后端两个维度提供针对跨域问题的解法，解释透彻。但需要开发者自行评估长远维护成本来决定采用哪种方案。

---

## 四、数据存储与可视化

### 7. DBeaver 连接 SQLite 数据库

* **提示词**：
  > 我现在在 dbeaver 上面进行 sqlite, 怎么连接？
* **AI 原始输出关键部分**：
  > “In DBeaver 中连接该项目的 SQLite 数据库非常简单：1. 打开 DBeaver，点击左上角的“新数据库连接” 2. 搜索并选择 SQLite 3. 在 Path (路径) 这一栏点击浏览，导航到项目目录 `/back/` 选择 `data.db` 文件 4. 点击测试连接并完成。”
* **人工修改说明**：
  按照 AI 提供的步骤，人工在 DBeaver 可视化工具中配置了相应的驱动，连接了后端自动生成的 `data.db` 数据库文件。同时依据 AI 建议，在手动修改数据后及时进行了 Commit（提交），防止了后端运行时遇到 `database is locked` 错误。
* **反思与收获**：
  AI 对主流数据库可视化管理工具（如 DBeaver、Navicat）的使用指南非常清晰，极大降低了对内嵌式轻量级数据库（SQLite）的可视化操作门槛。

### 8. GitHub 仓库中的 Git 子模块误识别问题

* **提示词**：
  > 问题7：将前端代码提交到 GitHub 仓库后，frontend 文件夹在网页上显示为带箭头的子模块，而不是普通文件夹，导致内部文件不可见。这是什么原因？如何修复这种 Git子模块误识别的问题？
* **AI 原始输出关键部分**：
  > “表示该文件夹被识别为 Git 子模块 (submodule)... 这是因为在提交时，`frontend` 文件夹内包含了一个独立的 Git 仓库（有 `.git` 子目录）。修复方法：删除 `frontend` 内的 `.git` 隐藏文件夹... 终端执行 `git rm --cached frontend` ... 重新添加 `git add frontend/` 并提交。”
* **人工修改说明**：
  排查了代码库存储状态，确认了 `frontend` 目录内部确实意外残留了前项模板的 `.git` 文件。清除了违规文件后，重建了索引并将代码正常推送到远程仓库进行存储展示。
* **反思与收获**：
  Git 版本控制系统的嵌套层级错误往往隐蔽性极高。AI 能够根据 GitHub 页面上一个简单的“带箭头的文件夹”视觉特征，瞬间反推底层的 Git 存储状态问题，这是非常强悍的排错能力。
"""
